# Vision-specific dependencies (base-ml already has: fastapi, uvicorn, torch, Pillow)
# Pin torch version to match base-ml to avoid re-downloading
torch==2.1.1
transformers==4.46.2
accelerate==1.1.1
easyocr==1.7.2
google-generativeai==0.3.2
openai==1.54.0  # For xAI Grok API (OpenAI-compatible)
